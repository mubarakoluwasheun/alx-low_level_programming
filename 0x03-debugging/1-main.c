#include <stdio.h>

/**
* main - causes an infinite loop
* Return: 0
*/

int main(void)
{
	/*
	int i;
	*/
	printf("Infinite loop incoming :(\n");

	/* the putchar will cause an infinite loop
	i = 0;

	while (i < 10)
	{
		putchar(i);
	so it was comment out here
	}
	*/

	printf("Infinite loop avoided! \\o/\n");

	return (0);
}
